export default function Title({name}) {
    return(
        <div>
            <h2 className="text-uppercase custom-text-secondary">{name}</h2>
            <div className="line mt-1"></div>
        </div>
    )
}